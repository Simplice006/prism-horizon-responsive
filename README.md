# prism-horizon-responsive
Responsive version of the Prism Horizon website — built on the same semantic HTML prototype, now styled with an external, mobile-first CSS stylesheet. Uses Flexbox and Grid for layout, media queries for wider screens, and follows accessibility practices like visible focus states and readable colour contrast.
